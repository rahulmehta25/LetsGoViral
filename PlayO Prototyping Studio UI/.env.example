# Backend API URL — use the deployed Cloud Run URL or localhost for local dev
VITE_API_URL=http://localhost:8080

# API key — must match MVP_API_KEY in backend/api-service/.env
VITE_API_KEY=<same-as-MVP_API_KEY>
