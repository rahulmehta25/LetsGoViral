# ─── Root .env.example — copy per service ─────────────────────────────────
# See backend/api-service/.env.example for full API service config
# See backend/video-processor/.env.example for full video-processor config
# See "PlayO Prototyping Studio UI/.env.example" for web app config

# ─── Shared GCP Settings ──────────────────────────────────────────────────
GCP_PROJECT_ID=clipora-487805
GCP_REGION=us-east1

# ─── Buckets ──────────────────────────────────────────────────────────────
GCS_UPLOADS_BUCKET=clipora-487805-uploads
GCS_PROCESSED_BUCKET=clipora-487805-processed
CDN_BASE_URL=https://storage.googleapis.com/clipora-487805-processed

# ─── Database ─────────────────────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=creator_mvp
DB_USER=clipora
DB_PASSWORD=<your-local-db-password>

# ─── Auth ─────────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
MVP_API_KEY=<generate-with-openssl-rand-hex-32>

# ─── Signed URL support for ADC (teammates without service account key) ──
# Set to the SA email; your Google account needs "Service Account Token Creator".
# Leave blank if using GOOGLE_APPLICATION_CREDENTIALS (key file).
GCS_SIGNING_SERVICE_ACCOUNT=

# ─── CORS (comma-separated origins, blank = allow all) ───────────────────
CORS_ORIGINS=

# ─── Mobile App ───────────────────────────────────────────────────────────
EXPO_PUBLIC_API_URL=https://clipora-api-594534640965.us-east1.run.app
EXPO_PUBLIC_API_KEY=<same-as-MVP_API_KEY>
